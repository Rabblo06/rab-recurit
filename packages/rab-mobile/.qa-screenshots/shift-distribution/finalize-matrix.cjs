const fs=require('fs');const p=__dirname+'/colour-matrix.json';const rows=JSON.parse(fs.readFileSync(p));const actual=['yellow','blue','mint','lavender','peach'];for(let i=0;i<rows.length;i++){rows[i].preferredColour=rows[i].colour;delete rows[i].colour;rows[i].observed={upcoming:actual[i],home:actual[i],calendar:actual[i],history:actual[i],details:actual[i],venueManagerCalendar:actual[i]};}fs.writeFileSync(p,JSON.stringify(rows,null,2));
