<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks cleartext (plain HTTP) traffic by default since API 28.
  rab-server has no HTTPS story in local dev, so cleartext is allowed only
  for the two hosts a dev/emulator build ever talks to: 10.0.2.2 (the
  Android emulator's alias for the host machine) and localhost (a physical
  device on the same network via adb reverse, or a Genymotion-style
  emulator). Any other host still requires HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
