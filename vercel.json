{
  "buildCommand": "npx nx build rab-front",
  "outputDirectory": "../../dist/packages/rab-front",
  "installCommand": "corepack enable && yarn install",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
